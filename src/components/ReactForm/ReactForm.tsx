// import './ReactForm.scss';

export const ReactForm = () => {
  // const navigate = useNavigate();
  // const { page } = useParams<{ page: string }>();

  return (
    <main className="main">
      <form action="">ReactForm</form>
    </main>
  );
};
