// import './Form.scss';

export const Form = () => {
  // const navigate = useNavigate();
  // const { page } = useParams<{ page: string }>();

  return (
    <main className="main">
      <form action="">Form</form>
    </main>
  );
};
